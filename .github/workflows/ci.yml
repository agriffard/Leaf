name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore Leaf.slnx
      - name: Build
        run: dotnet build Leaf.slnx --configuration Release --no-restore
      - name: Test
        run: dotnet test Leaf.slnx --configuration Release --no-build
